import { Button } from '@components';

import { MainContainer } from './styles';

const index = () => {
  return (
    <MainContainer>
      <Button label="버튼" />
    </MainContainer>
  );
};

export default index;
